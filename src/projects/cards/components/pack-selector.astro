---
// Pack selector - options populated by client-side JavaScript
---

<div class="pack-selector">
  <label for="pack-select">Pack:</label>
  <select id="pack-select" class="pack-selector__select"></select>
</div>

<style>
  .pack-selector {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .pack-selector__select {
    font: inherit;
    padding: 0.25rem 0.5rem;
    border: 1px solid var(--color-stroke);
    border-radius: var(--radius-control);
    background: var(--color-bg);
    color: var(--color-fg);
    cursor: pointer;
  }
</style>
