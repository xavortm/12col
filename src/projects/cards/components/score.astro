---
// Score display - points per pair set by client-side JavaScript
---

<div class="score" id="game-score">
	<div>
		<span class="score__pair" id="score-pair" aria-label="Pair score">0</span>
		<span class="score__times-sign">x</span>
		<span class="score__multiplier" id="score-multiplier" aria-label="Score multiplier">0</span>
	</div>

	<span class="score__value" id="score-value" aria-labelledby="score-label">0</span>
</div>

<style>
	span {
		display: inline-block;
		font-variant-numeric: tabular-nums;
		padding: 0.125em 0.25em;
		border-radius: 0.25em;
		font-weight: 900;
		color: white;
		min-width: 3ch;
		text-align: center;
	}

	.score__times-sign {
		min-width: auto;
		font-weight: normal;
		opacity: 0.5;
	}

	.score {
		display: flex;
		gap: 0.5rem;
		align-items: center;
		justify-content: space-between;
		font-size: 2em;
	}

	.score__value {
		min-width: 3ch;
	}

	.score__pair {
		background-color: var(--color-pair-score, #2563eb);
	}

	.score__multiplier {
		background-color: var(--color-multiplier, #dc2626);
	}
</style>
